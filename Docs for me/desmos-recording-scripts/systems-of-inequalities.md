# Recording script — Systems of Inequalities (shading trick)

- **Slug:** `systems-of-inequalities` · **Target:** SAT Desmos systems of inequalities
- **Output:** `public/images/desmos/systems-of-inequalities.{mp4,webm}` · ~20s · autoplay/muted/loop (no audio)
- **Demo problem:** Which point satisfies both y > x + 1 and y < −x + 5: (0, 4) or (3, 3)? **(Answer: (0, 4))**

## Exact Desmos input (two inequalities + the two candidate points)
```
y > x + 1
y < -x + 5
(0, 4)
(3, 3)
```

## Shot list (~20s, silent)
- **0:00** Blank Desmos calculator.
- **0:03** Type `y > x + 1` — shaded region appears.
- **0:07** Type `y < -x + 5` — second shading; the **overlap** is darker.
- **0:12** Type `(0, 4)` — point lands **inside** the double-shaded overlap.
- **0:15** Type `(3, 3)` — point lands **outside** the overlap.
- **0:18** Hold so both points + the overlap are visible, loop.

## Final frame must show
Both points plotted: **(0, 4)** clearly inside the double-shaded overlap region, **(3, 3)** outside it — so (0, 4) is the answer.

## After recording
Save as `public/Videos/systems-of-inequalities.mov` and hand it to me.
