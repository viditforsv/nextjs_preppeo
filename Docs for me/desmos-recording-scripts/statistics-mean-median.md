# Recording script — Mean, Median & Statistics

- **Slug:** `statistics-mean-median` · **Target:** SAT Desmos mean and median
- **Output:** `public/images/desmos/statistics-mean-median.{mp4,webm}` · ~20s · autoplay/muted/loop (no audio)
- **Demo problem:** Data set 4, 8, 6, 10, 7. Find the median. **(Answer: 7)**

## Exact Desmos input (three lines)
```
L = [4, 8, 6, 10, 7]
median(L)
mean(L)
```

## Shot list (~20s, silent)
- **0:00** Blank Desmos calculator.
- **0:03** Type `L = [4, 8, 6, 10, 7]`.
- **0:08** Type `median(L)` → Desmos prints **7** (it sorts for you).
- **0:13** Type `mean(L)` → prints **7** too — nice bonus showing both functions.
- **0:17** Hold so both `median(L) = 7` and `mean(L) = 7` are readable, loop.

## Final frame must show
`median(L) = 7` clearly visible (the answer), ideally with `mean(L) = 7` beneath it.

## Tip
You could add `stdev(L)` / `total(L)` as a quick flash of "more built-ins," but keep it ~20s — median is the headline.

## After recording
Save as `public/Videos/statistics-mean-median.mov` and hand it to me.
