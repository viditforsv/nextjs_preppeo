# Recording script — Solve Absolute Value Equations

- **Slug:** `absolute-value` · **Target:** SAT Desmos absolute value equations
- **Output:** `public/images/desmos/absolute-value.{mp4,webm}` · ~20s · autoplay/muted/loop (no audio)
- **Demo problem:** Solve |2x − 3| = 5. **(Answer: x = 4 or x = −1)**

## Exact Desmos input (two lines)
```
y = abs(2x - 3)
y = 5
```
(Typing `|2x - 3|` works too — Desmos shows the bars; `abs()` is the safe keyboard way.)

## Shot list (~20s, silent)
- **0:00** Blank Desmos calculator.
- **0:03** Type `y = abs(2x - 3)` — the **V-shape** draws.
- **0:08** Type `y = 5` — a horizontal line cutting the V at two points.
- **0:12** Click the **right** intersection → **(4, 5)**.
- **0:15** Click the **left** intersection → **(−1, 5)**.
- **0:18** Hold both labels visible (x = 4 and x = −1), loop.

## Final frame must show
Both intersection points labelled **(4, 5)** and **(−1, 5)** — the two x-values, 4 and −1, are the solutions.

## After recording
Save as `public/Videos/absolute-value.mov` and hand it to me.
